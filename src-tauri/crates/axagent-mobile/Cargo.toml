[package]
name = "axagent-mobile"
version.workspace = true
edition.workspace = true
license.workspace = true

[lib]
crate-type = ["staticlib"]

[dependencies]
axagent_lib = { path = "../..", package = "axagent" }
