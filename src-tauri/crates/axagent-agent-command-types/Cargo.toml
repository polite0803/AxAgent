[package]
name = "axagent-agent-command-types"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
inventory = "0.3"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
tauri = { version = "2", optional = true }

[features]
default = []
tauri-integration = ["tauri"]
