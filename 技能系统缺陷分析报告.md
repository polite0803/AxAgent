---
AIGC:
    Label: "1"
    ContentProducer: 001191440300708461136T1XGW3
    ProduceID: 4bf27337f30a147f24e73352c7fc3b8c_b5810f717ad411f198065254006c9bbf
    ReservedCode1: /7FMfwVDifOxpqJ8f8+q+likBkvYTg9BHIpgloXNdm0CA34T3xGCqEqyDcR0QTOOiDxtDiywPCliEd7xEE45bWicpLMmFTqLzGDNNBalvz9x6+SkjSWdl/1qKMEnxa9IUW+Sp/97JWiyTBULz38NCzJWXi8vy3xOh5BUNGaTxAQpb0qaM3h5QdGJ42c=
    ContentPropagator: 001191440300708461136T1XGW3
    PropagateID: 4bf27337f30a147f24e73352c7fc3b8c_b5810f717ad411f198065254006c9bbf
    ReservedCode2: /7FMfwVDifOxpqJ8f8+q+likBkvYTg9BHIpgloXNdm0CA34T3xGCqEqyDcR0QTOOiDxtDiywPCliEd7xEE45bWicpLMmFTqLzGDNNBalvz9x6+SkjSWdl/1qKMEnxa9IUW+Sp/97JWiyTBULz38NCzJWXi8vy3xOh5BUNGaTxAQpb0qaM3h5QdGJ42c=
---

# 技能系统缺陷分析报告

> 项目: AxAgent | 生成时间: 2026-07-08 | 分析范围: 前端 + Rust 后端全链路

---

## 一、总览

扫描范围覆盖技能系统的全部关键模块：生命周期管理、权限校验、状态管理（前后端）、技能匹配、遗传进化引擎、沙箱执行、技能发现/安装、市场导入导出。

**缺陷分类统计**

| 严重程度 | 数量 | 说明                                             |
| -------- | ---- | ------------------------------------------------ |
| 🔴 高危  | 6    | 可能导致数据安全、状态损坏或无法恢复的运行时错误 |
| 🟡 中危  | 12   | 逻辑漏洞、性能隐患、边界条件缺失                 |
| 🟢 低危  | 7    | 代码质量问题、维护性缺陷、死代码                 |

---

## 二、缺陷详述

### 🔴 高危缺陷

#### 2.1 技能卸载时序错误：生命周期钩子先于物理删除执行且无回滚

**文件**: `src/stores/feature/skillStore.ts` — `uninstallSkill` 方法

```typescript
uninstallSkill: async (name: string) => {
  const { triggerOnUninstall } = await import("@/lib/skillLifecycle");
  await triggerOnUninstall(name).catch(logIpcError("triggerOnUninstall"));
  await invoke("uninstall_skill", { name });  // 钩子执行后才删除
  ...
}
```

**问题**: `triggerOnUninstall` 中的钩子（如 `executeHooksSequential`）可能执行声明式 action 调用 `invoke`。如果钩子中的某个 action 执行失败（例如清理资源失败），技能已经部分卸载但仍可被物理删除。而且错误仅被 `logIpcError` 吞掉，用户完全不知道钩子失败。

**风险**: 钩子失败后技能仍被物理删除，可能导致孤儿资源、挂载点泄漏。

**建议**: 钩子失败后应阻止卸载并通知用户；或将钩子执行结果作为卸载的前置条件。

---

#### 2.2 权限完整性检查对"空权限"存在歧义

**文件**: `src/lib/skillPermissions.ts` — `computePermissionHash`

```typescript
async function computePermissionHash(
  permissions: SkillPermissions | undefined,
): Promise<string> {
  const payload = JSON.stringify(permissions ?? {}, Object.keys(permissions ?? {}).sort());
  ...
}
```

**问题**: 当 `permissions` 为 `undefined`（技能未声明权限）和 `permissions: {}`（技能声明了空权限对象）时，`computePermissionHash` 对两者生成完全相同的哈希值。这意味着 `checkPermissionIntegrity` 无法区分"从未声明过权限"和"声明了空权限"这两种截然不同的安全语义。

**风险**: 如果技能原本有权限声明，更新后去掉了权限声明（`undefined`），完整性检查会因为哈希相同而不报警。

**建议**: 使用 sentinel 值区分 `undefined` 与空对象，例如 `JSON.stringify(permissions === undefined ? "__NO_PERMISSIONS__" : permissions, ...)`。

---

#### 2.3 进化引擎 `run()` 可能陷入无限循环

**文件**: `src-tauri/crates/trajectory/src/skill_evolution.rs` — `run` 方法

```rust
for _generation in 0..self.config.max_generations {
    if let Some(ref pop) = self.population {
        if pop.is_converged(&self.config) { break; }
        if pop.generation >= self.config.max_generations as u32 { break; }
    }
    if self.evolve_generation_v2(test_trajectories).await.is_none() {
        break;
    }
}
```

**问题**: `evolve_generation_v2` 在群体已存在但 generation 未递增时（例如 `evolve` 内部的 `before_gen` 和 `pop.generation` 相等时，即群体已收敛但 `is_converged` 返回 false 的边界情况）可能返回 `Some(...)`。此时 for 循环会继续，但 population 状态没有任何变化，导致 CPU 空转 `max_generations` 次。

**风险**: `max_generations` 默认 50，每代都可能做 LLM 调用和沙箱验证。在 `use_llm_mutation=true` 且 `use_execution_validation=true` 时，每代会执行昂贵的 LLM 和沙箱调用，极端情况可能耗尽 token 配额。

**建议**: 在 `evolve_generation_v2` 内部或 `run` 循环中增加"连续无变化计数器"，连续 N 代无变化则提前退出。

---

#### 2.4 技能安装 ID 生成使用 Relaxed 内存序 + 毫秒时间戳，存在碰撞风险

**文件**: `src-tauri/crates/trajectory/src/skill_manager.rs`

```rust
static SKILL_ID_COUNTER: AtomicU64 = AtomicU64::new(0);

fn generate_skill_id() -> String {
    let timestamp = Utc::now().timestamp_millis();
    let counter = SKILL_ID_COUNTER.fetch_add(1, Ordering::Relaxed);
    format!("skill_{}_{}", timestamp, counter)
}
```

**问题**: `Ordering::Relaxed` 不提供跨线程的 happens-before 保证。在高并发场景下（如批量导入技能、并行安装），两个线程可能读取到相同的 `timestamp_millis()` 且由于 Relaxed 内存序读到相同的 counter 值（或 counter 乱序），产生 ID 碰撞。

**风险**: ID 碰撞导致 `SkillManager` 的 HashMap 中后插入的技能覆盖先插入的技能，造成数据丢失。

**建议**: 使用 `Ordering::SeqCst`；或直接用 `Uuid::new_v4()`（项目中已依赖 uuid crate）。

---

#### 2.5 沙箱 iframe 事件处理器泄漏

**文件**: `src/components/skill/SkillSandboxContainer.tsx`

```typescript
window.addEventListener("message", messageHandler);
// messageHandler 只在自身内部（收到 skill:ready 时）移除自己
```

**问题**: 如果 iframe 因任何原因（脚本错误、网络超时、src 为空）永不发送 `skill:ready` 消息，`messageHandler` 将永久驻留在 `window` 上。组件的 `useEffect` 清理函数只清理 `bridgeRef` 和 `apiBridgeRef`，**不清理事件监听器**。

**风险**: 每次组件重新挂载（如主题切换、skill 重新加载）都会新增一个未清理的监听器，累积内存泄漏且可能导致重复处理消息。

**建议**: useEffect 的 return 清理函数中显式调用 `window.removeEventListener("message", messageHandler)`。

---

#### 2.6 插件技能目录命名冲突

**文件**: `src-tauri/crates/plugins/src/skill_installer.rs`

```rust
fn sanitize_for_path(id: &str) -> String {
    id.chars()
        .map(|ch| match ch { '/' | '\\' | '@' | ':' => '-', other => other })
        .collect()
}
```

**问题**: 两个不同插件 ID（如 `@clawd/ths@external` 和 `-clawd-ths-external`）经过 `sanitize_for_path` 后会映射到完全相同的目录名。后安装的插件会覆盖先安装的插件技能文件。

**风险**: 技能覆盖导致功能异常或安全隐患（恶意构造插件 ID 覆盖正常技能）。

**建议**: 使用 base64 编码或 hash 作为目录名的一部分，或在冲突时检测并拒绝。

---

### 🟡 中危缺陷

#### 2.7 技能匹配器永不填充 `match_reasons`

**文件**: `src-tauri/crates/trajectory/src/skill_matcher.rs` — `calculate_match_score`

```rust
let reasons: Vec<String> = Vec::new();  // 初始化
// ... 大量匹配逻辑 ...
// 函数结束：从未 push 到 reasons
(score, reasons)  // reasons 永远是空 Vec
```

**问题**: `match_reasons` 字段被创建但从未被写入任何值。`SkillMatch` 结构体返回时 `match_reasons: Vec::new()` 永远是空的。UI 层依赖此字段做调试展示时将看不到任何匹配原因。

**建议**: 在每个 `score += ...` 分支中添加 `reasons.push(...)`。

---

#### 2.8 进化引擎的 LLM 质量评估函数形同虚设

**文件**: `src-tauri/crates/trajectory/src/skill_evolution.rs` — `DefaultLlmEvolutionProvider::evaluate_quality`

```rust
let score = (content.len() as f64 / 500.0).min(1.0) * 0.3
    + if content.contains("error") || content.contains("Error") { 0.3 } else { 0.0 }
    + if content.contains("verify") || content.contains("check") { 0.2 } else { 0.0 }
    + 0.2;
```

**问题**: 基于内容长度和关键词匹配的评分函数完全不反映技能的实际质量。较长的内容、包含 "error" 字符串的技能会获得虚高的质量评分。`DefaultLlmEvolutionProvider` 是后备实现，但被标注为 "LLM-driven semantic mutation" 的一部分，容易误导开发者认为这是一种有效的评估方式。

**建议**: 要么明确标注为 "仅用于测试/演示"；要么改为调用实际的 LLM 评估。

---

#### 2.9 前端 `refreshSkill` 中技能合并逻辑有缺陷

**文件**: `src/stores/feature/skillExtensionStore.ts` — `refreshSkill`

```typescript
const updatedSkills = skills.map((s) => {
  const existing = skillMap.get(s.name);
  return existing && s.name !== skillName ? existing : s;
});
```

**问题**: 当后端返回的 `skills` 中包含了一个"名称与另一个已有技能相同"的新技能（改名场景），逻辑是：`s.name !== skillName` 为 true，取 `existing`。但这意味着新数据被丢弃，UI 永远不更新。

**建议**: 直接使用后端最新的 skills 列表，无需做增量合并；或改用以 skill ID 为键。

---

#### 2.10 `toggleSkill` 乐观更新缺少竞态保护

**文件**: `src/stores/feature/skillStore.ts` — `toggleSkill`

```typescript
toggleSkill: async (name: string, enabled: boolean) => {
  set({ skills: get().skills.map(...) });  // 乐观更新
  try {
    await invoke("toggle_skill", { name, enabled });
    // 如果这里失败，回滚...
  } catch (e) {
    set({ skills: get().skills.map(...) });  // 回滚
  }
}
```

**问题**: 如果用户快速连续切换同一个技能（双击），第二次 `set` 读到的 `get().skills` 可能已经是第一次乐观更新后的状态。如果第一次 invoke 失败并回滚，但第二次 invoke 已发出，最终状态不可预测。

**建议**: 使用操作队列或 debounce，或使用 rev 版本号检测。

---

#### 2.11 技能卸载组 (`uninstallSkillGroup`) 使用 `Promise.all` 但无并发控制

**文件**: `src/stores/feature/skillStore.ts`

```typescript
await Promise.all(
  groupSkills.map((skill) => triggerOnUninstall(skill.name)...),
);
```

**问题**: 对于大型技能组（如 20+ 个技能），`Promise.all` 同时触发所有卸载钩子。如果钩子中包含文件 I/O 或系统调用，可能导致资源竞争。而且 `invoke` 调用走 Tauri IPC，大量并发可能压垮后端。

**建议**: 使用 `p-limit` 或手写分批并发控制（如每次最多 5 个）。

---

#### 2.12 技能执行模式 `execution_mode` 死代码

**文件**: `src-tauri/src/commands/agent/skill_execution.rs` — `execute_skill_async`

```rust
let execution_mode = "content".to_string();
// ...
match execution_mode.as_str() {
    "mcp" => { /* MCP 分支 */ },
    _ => { /* content 分支 */ },
}
```

**问题**: `execution_mode` 被硬编码为 `"content"`，MCP 执行分支永远不可达。这意味着 `extract_mcp_tool_call` 解析出的 MCP 工具调用信息永远不会被实际执行。

**风险**: 技能定义中包含 MCP 工具调用的技能实际上被"降级"为内容注入模式，功能不完整。

**建议**: 根据 skill manifest 中的声明动态确定 execution_mode，或直接删除 MCP 分支。

---

#### 2.13 技能输出追踪器全局单例无内存上限

**文件**: `src-tauri/src/commands/agent/skill_execution.rs`

```rust
static SKILL_OUTPUT_TRACKER: std::sync::OnceLock<SkillOutputTracker> = ...;
```

**问题**: `SkillOutputTracker` 是全局 `OnceLock`，内部 HashMap 以 `conversation_id` 为键，但 `conversation_id` 对应的 `Vec<SkillExecutionRecord>` 永远不会被清理。长时间运行后内存持续增长。

**建议**: 增加 LRU 驱逐策略或 TTL 过期机制。

---

#### 2.14 进化引擎中 `tournament_select` 对空样本集返回占位 genome

**文件**: `src-tauri/crates/trajectory/src/skill_evolution.rs`

```rust
if population.is_empty() {
    return SkillGenome {
        skill_id: String::new(),
        content: String::new(),
        ...
    };
}
```

**问题**: 当 population 为空时，返回一个全空的 `SkillGenome`。调用方（`evolve` 方法中的 `tournament_select`）不会检查返回的 genome 是否有效，直接将空 genome 加入下一代种群。这会导致后续的代中出现无效个体，污染整个进化过程。

**建议**: 空种群应直接 panic（这是不应发生的内部状态错误）或返回 `Option<SkillGenome>` 让调用方处理。

---

#### 2.15 技能权限校验只检查命令白名单，不校验 store/navigate/network 的实际使用

**文件**: `src/lib/skillPermissions.ts` — `validateSkillPermissions`

**问题**: 该函数只检查 `requiredCommands`（从 capabilities 中提取的 invoke 命令）是否在 `permissions.commands` 白名单中。但 `permissions.storeRead`、`permissions.storeWrite`、`permissions.navigate`、`permissions.network` 等字段仅在声明"完全缺失"时被标记，从未被验证是否与实际使用时匹配。

例如，技能声明了 `storeWrite: ["settings"]`，但实际代码中写入了 `chat` store——权限校验无法检测到这种越权行为。

**建议**: 在沙箱的 RPC 桥接层增加运行时权限校验，每次 store write 都对照白名单。

---

#### 2.16 进化引擎 `crossover_genomes` 边界条件可产生空 steps

```rust
let cross_point = rng.gen_range(1..parent1.steps.len().max(2)).min(parent1.steps.len());
```

**问题**: 当 `parent1.steps.len() == 1` 时，`1..2` 产生 `cross_point = 1`，`parent1.steps[..1]` 取第一个元素。但如果 `parent2.steps.len() == 1`，`parent2.steps[1..]` 为空切片，子代只有一个步骤。虽然不会 panic，但交叉操作退化成了简单的复制。

**建议**: 增加最小步骤数检查，steps 不足时跳过交叉。

---

#### 2.17 技能市场搜索缓存的驱逐策略使用"最早插入"而非"最久未使用"

**文件**: `src-tauri/tests/skills_tests.rs` — `SimCache::set`（模拟实际代码逻辑）

**问题**: 模拟的驱逐策略使用 `sort_by_key(|(_, (_, ts))| *ts)` 驱逐"最早插入"的条目，但这实际是 FIFO 而非 LRU。条目一旦插入，即使经常被访问，也会因时间长而被驱逐。

**建议**: 更新访问时间戳，驱逐时按访问时间排序（LRU）。

---

#### 2.18 技能依赖检查前端组件类型定义不可靠

**文件**: `src/components/skill/SkillDependencyCheck.tsx`

```typescript
const deps = skill.manifest?.dependencies;
for (const [depName, versionConstraint] of Object.entries(deps)) {
```

**问题**: 前端 `Skill` 类型中，`manifest.dependencies` 被定义为 `Record<string, string>`，但后端 Rust 的 `SkillDependency` 结构体包含 `name`、`version_constraint`、`required` 三个字段。前后端类型定义不一致。如果后端返回的是结构体数组而非 record，前端 `Object.entries` 会静默失败（不报错但不展示有效依赖）。

**建议**: 统一前后端依赖数据格式，或在前端增加格式检测和转换逻辑。

---

### 🟢 低危缺陷

#### 2.19 生命周期缓存 TTL 固定 5 分钟无配置项

**文件**: `src/lib/skillLifecycle.ts`

```typescript
const LIFECYCLE_CACHE_TTL_MS = 5 * 60 * 1000;
```

**问题**: 硬编码的 5 分钟 TTL，且只在 `readLifecycleData` 读取时检查。如果技能在 5 分钟内被修改多次（如开发调试时），缓存会返回过期数据。

---

#### 2.20 `skillEventBus` 的 FIFO 驱逐策略不可靠

**文件**: `src/lib/skillEventBus.ts`

```typescript
const keys = listeners.keys();
// Map 的 keys() 迭代顺序是插入顺序（ES6 规范）
```

虽然 ES6 规范规定 Map 的迭代顺序是插入顺序，FIFO 驱逐是有效的，但驱逐的是"整个 key 的所有监听器"而非单个监听器。如果某个 Skill 注册了多个事件但只有一个不活跃，所有事件监听器都会被删除。

---

#### 2.21 `SkillIcon` 回退图标选择不当

**文件**: `src/lib/skillIcons.ts`

```typescript
console.warn(`[skillIcons] 未识别的图标 "${iconStr}"，回退到 Puzzle`);
return Puzzle;
```

**问题**: Puzzle 图标语义不明确，用户看到会困惑。建议使用更通用的图标（如 `Package`、`Box`）或允许 manifest 指定 fallback。

---

#### 2.22 `skillActionExecutor` 中自定义函数注册表无并发保护

**文件**: `src/lib/skillActionExecutor.ts`

```typescript
const customHandlers = new Map<string, CustomHandler>();
export function registerCustomHandler(handlerId: string, handler: CustomHandler) {
  customHandlers.set(handlerId, handler);
}
```

**问题**: `Map` 在 JavaScript 中是单线程操作安全，但如果有异步逻辑在 register 和 get 之间交叉，可能导致短暂的竞态。实际影响很小，主要是代码质量问题。

---

#### 2.23 `skills_hub_export` 中 manifest 发现逻辑脆弱

**文件**: `src-tauri/src/commands/skills_hub.rs`

```rust
let manifest_path = skill_path
    .join("manifest.json")
    .exists()
    .then(|| skill_path.join("manifest.json"))
    .or_else(|| {
        skill_path.join("skill-manifest.json")
            .exists()
            .then(|| skill_path.join("skill-manifest.json"))
    });
```

**问题**: 两次 `exists()` 调用之间存在 TOCTOU 竞态（文件在第一次 exists 之后被删除）。虽然在实际场景中几乎不会发生，但不符合防御性编程原则。

---

#### 2.24 技能分解缓存锁粒度过粗

**文件**: `src-tauri/src/commands/skill_decomposition.rs`

```rust
lazy_static! {
    pub static ref DECOMPOSITION_CACHE: tokio::sync::Mutex<DecompositionCache> =
        tokio::sync::Mutex::new(DecompositionCache::new(300));
}
```

**问题**: 使用 `tokio::sync::Mutex` 保护整个缓存，`.cleanup_expired()` 在每次 `preview_decomposition` 时都会在持有锁期间遍历并清理过期条目。在高并发下，缓存清理会阻塞其他请求。建议使用 `RwLock` 或分片锁。

---

#### 2.25 `SkillSandboxContainer` 中 `notification` 错误类型不匹配

**文件**: `src/components/skill/SkillSandboxContainer.tsx`

```typescript
notification[type]({ message, placement: "bottomRight" });
```

**问题**: `antd` 的 `notification` 对象的方法签名与 `SkillHostUi.notify` 的参数不完全一致（`notification.error()` 需要 `description` 等）。TypeScript 编译时可能不报错（因为类型推断），但运行时某些参数不被正确处理。

---

## 三、架构层面问题

### 3.1 双重技能存储导致数据同步困难

技能数据同时存在于两个地方：

- **Rust 后端**: `TrajectoryStorage` 中的 `Skill` 结构体（包含完整的元数据、遗传进化状态）
- **前端 Store**: `useSkillStore` + `useSkillExtensionStore`（两个独立 Zustand store 各自持有 skills 列表）

两个 store (`skillStore` 和 `skillExtensionStore`) 各有 `loadSkills()` / `fetchSkills()`，通过 `syncExtensionStore()` 手动同步。如果同步遗漏，UI 展示的技能列表和实际可用技能不一致。

**建议**: 统一为单一数据源，或至少用一个 store 做 source of truth，另一个只做派生数据。

### 3.2 技能发现机制静态化

**文件**: `src-tauri/crates/kit/src/skill_dirs.rs`

`SKILL_DIRS` 和 `EXTERNAL_DIRS` 都是 `LazyLock`（启动时计算一次）。运行时新增技能目录或修改 `~/.axagent/config.yaml` 中的 external_dirs 不会被感知，必须重启应用。

**建议**: 提供热重载机制或监听文件系统变更事件。

### 3.3 遗传进化引擎与技能系统耦合过紧

`SkillEvolutionEngine` 直接嵌入 `SkillState`，在应用启动时就初始化。进化引擎包含 LLM 调用、沙箱执行等重量级操作，但在大多数使用场景下根本不会被触发（需要足够的执行轨迹数据）。

**建议**: 将进化引擎改为可选的、懒初始化的插件，按需启动。

---

## 四、改进优先级建议 & 修复状态

| 优先级 | 缺陷编号             | 修复状态            | 修复复杂度 | 影响范围       |
| ------ | -------------------- | ------------------- | ---------- | -------------- |
| P0     | 2.1 (卸载时序)       | ✅ 已修复           | 低         | 数据安全       |
| P0     | 2.5 (沙箱泄漏)       | ✅ 已修复           | 低         | 内存泄漏       |
| P0     | 2.4 (ID碰撞)         | ✅ 已修复           | 低         | 数据丢失       |
| P1     | 2.2 (权限哈希)       | ✅ 已修复           | 低         | 安全           |
| P1     | 2.3 (进化死循环)     | ✅ 已修复           | 中         | 性能           |
| P1     | 2.6 (目录冲突)       | ✅ 已修复           | 中         | 功能异常       |
| P2     | 2.7 (match_reasons)  | ✅ 已修复           | 低         | 调试可用性     |
| P2     | 2.8 (LLM评分)        | ✅ 已标注           | 低         | 代码可读性     |
| P2     | 2.9 (refreshSkill)   | ✅ 已修复           | 低         | 数据一致性     |
| P2     | 2.10 (toggle竞态)    | ✅ 已修复           | 中         | 状态一致性     |
| P2     | 2.11 (并发控制)      | ✅ 已在P0-2.1中修复 | 低         | 资源竞争       |
| P2     | 2.12 (MCP死代码)     | ✅ 已标注           | 低         | 代码可维护性   |
| P2     | 2.13 (内存泄漏)      | ✅ 已修复           | 低         | 内存泄漏       |
| P2     | 2.14 (空genome)      | ⚠️ 需讨论            | 中         | 逻辑正确性     |
| P2     | 2.15 (权限校验)      | ✅ 已标注           | 高         | 安全（架构级） |
| P2     | 2.16 (crossover边界) | ⚠️ 已有防护          | 低         | 边缘情况       |
| P2     | 2.17 (FIFO→LRU)      | ⚠️ 需讨论            | 低         | 缓存效率       |
| P2     | 2.18 (依赖类型)      | ✅ 已修复           | 低         | 功能正确性     |
| P3     | 2.19 (TTL硬编码)     | ⚠️ 低优先级          | 低         | 开发体验       |
| P3     | 2.20 (eventBus)      | ⚠️ 低优先级          | 低         | 边缘情况       |
| P3     | 2.21 (图标回退)      | ✅ 已修复           | 低         | UX             |
| P3     | 2.22 (并发保护)      | ⚠️ 极低风险          | 低         | 代码质量       |
| P3     | 2.23 (TOCTOU)        | ⚠️ 极低风险          | 低         | 防御性编程     |
| P3     | 2.24 (锁粒度)        | ⚠️ 需架构重构        | 高         | 高并发性能     |
| P3     | 2.25 (notification)  | ⚠️ 低优先级          | 低         | 边缘情况       |

### 修复汇总

- **已修复**: 13 项（P0: 3, P1: 3, P2: 6, P3: 1）
- **已标注/文档化**: 4 项（P2: 2.8 LLM评分, P2: 2.12 MCP死代码, P2: 2.15 权限校验, P3: 2.23 TOCTOU）
- **需进一步讨论**: 8 项（多为架构级改动或极低风险项）

### 冲突处理说明

本次修复未发现任何修复之间的冲突。所有改动均保持向后兼容：

- `skillLifecycle.ts` 的 `triggerOnUninstall` 仅在钩子存在且失败时才抛出错误（无钩子技能不受影响）
- `skill_installer.rs` 的目录名变更仅影响新安装的插件（已有目录不受影响）
- 前端 store 改动均保持 API 契约不变
  _（内容由AI生成，仅供参考）_

---

## 五、架构级遗留问题修复方案分析

> 生成时间: 2026-07-08 | 对应第二步分析任务

### 5.1 全局追踪器 LRU 改造方案 (对应缺陷 2.13)

**问题现状**

`SkillOutputTracker`（`src-tauri/src/commands/agent/skill_execution.rs`）是一个全局 `OnceLock` 单例。内部结构为：

```
Mutex<HashMap<conversation_id, Vec<SkillExecutionRecord>>>
```

在上一轮修复中，已添加了每个 conversation 的记录上限（`max_records_per_conv: 200`），超出时丢弃最旧记录。但 **conversation_id 级别的 HashMap 没有驱逐机制**——每产生一个新会话就会新增一个 key，长期运行后 `conversation_id` 的 HashMap 持续膨胀。

**影响评估**

| 维度       | 评估                                                                                                                                           |
| ---------- | ---------------------------------------------------------------------------------------------------------------------------------------------- |
| 内存风险   | 中等。假设每个 conversation 平均 10 条 record，每条 ~200 字节，1 万个 conversation 仅约 20MB。但长期运行（数周不重启）可能积累到 10 万+ 会话。 |
| 性能风险   | 低。HashMap 查找是 O(1)，不随 key 数量线性退化。                                                                                               |
| 修复紧迫性 | P3（长期运行场景才触发）                                                                                                                       |

**推荐方案：conversation 级别 LRU + 定时清理**

```rust
// 改造后的数据结构
use std::collections::{HashMap, VecDeque};

pub(super) struct SkillOutputTracker {
    inner: Mutex<TrackerState>,
    max_conversations: usize,   // 新增：全局 conversation 上限
}

struct TrackerState {
    conversations: HashMap<String, ConversationRecords>,
    lru_order: VecDeque<String>,  // LRU 顺序：最近访问在末尾
}

struct ConversationRecords {
    records: Vec<SkillExecutionRecord>,
    max_records: usize,
    last_access: Instant,         // 新增：最后访问时间
}
```

**改动要点**：

1. 所有访问路径（`record_execution`、`get_recent_skills`、`update_output`）均更新对应 conversation 的 `last_access` 时间戳
2. 每次 `record_execution` 新增 key 时检查 `conversations.len()`，超出 `max_conversations`（建议默认 2000）时驱逐最久未访问的 conversation
3. 额外添加 `cleanup_stale_conversations()` 方法，按 TTL（如 24 小时无访问）清理，由定时器或惰性触发

**风险与成本**：

- 改动成本：**低**。`SkillOutputTracker` 是模块内部类型，无公开 API 变更
- 向后兼容：完全兼容，仅增加容量上限
- 测试需求：需要手动测试多 conversation 场景

**替代方案**：

- 方案 B（最小改动）：仅在 `record_execution` 新增 key 时检查 `conversations.len()`，超限时直接删除最早的 key（FIFO）。成本最低，但不精确。
- **推荐方案 A（LRU）**：改动量适中，语义精确，长期维护成本低。

---

### 5.2 技能发现热重载方案 (对应缺陷 3.2)

**问题现状**

`src-tauri/crates/kit/src/skill_dirs.rs` 中，`SKILL_DIRS` 和 `EXTERNAL_DIRS` 都使用 `LazyLock` 实现：

```rust
static SKILL_DIRS: LazyLock<Vec<(String, PathBuf)>> = LazyLock::new(|| {
    // 启动时计算一次，之后永不更新
});
static EXTERNAL_DIRS: LazyLock<Vec<PathBuf>> = LazyLock::new(load_external_dirs_from_config);
```

这意味着：

- 用户在 `~/.axagent/config.yaml` 中新增 `external_dirs` 后，必须重启应用才能生效
- 运行时新增技能目录（如 `~/.claude/skills/new-skill/`）不会被感知
- 插件安装后创建的目录不会出现在已经运行的实例中

**影响评估**

| 维度       | 评估                                               |
| ---------- | -------------------------------------------------- |
| 用户体验   | 中。开发者频繁调整技能目录时需要重启，影响调试效率 |
| 功能正确性 | 低。已安装技能本身不受影响，仅新发现受影响         |
| 修复紧迫性 | P3（开发体验优化）                                 |

**推荐方案：`RwLock` + 重载命令 + 文件系统监听（渐进式）**

**阶段一（低成本，推荐立即执行）**：

```rust
// 将 LazyLock 替换为 RwLock
static SKILL_DIRS: LazyLock<RwLock<Vec<(String, PathBuf)>>> = 
    LazyLock::new(|| RwLock::new(compute_skill_dirs()));

// 对外接口保持不变
pub fn skill_dirs() -> Vec<(&'static str, PathBuf)> {
    // 读取时获取读锁
    let guard = SKILL_DIRS.read().unwrap();
    guard.iter().map(|(l, d)| (l.as_str(), d.clone())).collect()
}

// 新增：重载命令（通过 Tauri command 暴露）
pub fn reload_skill_dirs() -> Result<(), String> {
    let mut guard = SKILL_DIRS.write().map_err(|e| e.to_string())?;
    *guard = compute_skill_dirs();
    Ok(())
}
```

**阶段二（可选，后续推进）**：

使用 `notify` crate 监听 `~/.axagent/`、`~/.claude/`、`~/.axagent/config.yaml`，变更时自动调用 `reload_skill_dirs()`。

**改动要点**：

1. 函数签名不变（返回 `Vec<(&'static str, PathBuf)>`），但内部生命周期改为动态
2. `skill_dirs()` 和 `all_skills_dirs()` 现在从 `RwLock` 读锁获取数据，调用者拿到的是克隆
3. 新增 Tauri command `reload_skill_dirs` 暴露给前端
4. 所有现有调用者无需修改（`skill_dirs()` 函数签名不变）

**风险与成本**：

- 改动成本：**低**（阶段一仅 ~30 行改动，全部在 `skill_dirs.rs` 内）
- 向后兼容：完全兼容，所有公开 API 签名不变
- 潜在风险：并发重载时的短暂读延迟（RwLock 读锁开销可忽略）
- 测试需求：验证重载后现有调用者行为正常

---

### 5.3 进化引擎解耦方案 (对应缺陷 3.3)

**问题现状**

`SkillEvolutionEngine`（`src-tauri/crates/trajectory/src/skill_evolution.rs`）当前通过 `SkillState` 在应用启动时直接初始化。引擎内部持有 `LLM Provider` 和 `Sandbox Executor`，这些组件在大多数使用场景下不会被触发：

- 遗传进化需要**大量执行轨迹数据**（Trajectory）才能有效运作
- 大多数用户的技能使用模式是"安装 → 使用"，不会积累足够的失败/成功轨迹来触发进化
- 进化过程涉及 LLM 调用（昂贵）和沙箱验证（重量级），但在用户没有轨迹数据时完全是空转
- `run()` 方法每次都被调用，但 `evolve_generation_v2` 在无有效轨迹时只做简单的 fitness 评估（默认 0.5）

**影响评估**

| 维度       | 评估                                                                                           |
| ---------- | ---------------------------------------------------------------------------------------------- |
| 资源浪费   | 中等。启动时分配 `EvolutionPopulation` (20 个个体)，约占用数 KB 内存，但进化逻辑不会被实际触发 |
| 代码耦合   | 高。`SkillState` 直接依赖 `SkillEvolutionEngine`，无法独立测试技能管理逻辑                     |
| 修复紧迫性 | P3（架构优化）                                                                                 |

**推荐方案：懒初始化 + 按需触发**

```rust
// 改造前：SkillState 直接持有 SkillEvolutionEngine
// 改造后：使用 Arc<RwLock<Option<SkillEvolutionEngine>>>

pub struct SkillState {
    // ... 其他字段 ...
    evolution_engine: Arc<RwLock<Option<SkillEvolutionEngine>>>,
}

impl SkillState {
    /// 按需获取或初始化进化引擎
    pub async fn ensure_evolution_engine(
        &self,
        llm_provider: Option<Arc<dyn LlmEvolutionProvider>>,
        sandbox: Option<Arc<dyn SandboxExecutor>>,
    ) -> bool {
        let mut engine_guard = self.evolution_engine.write().await;
        if engine_guard.is_none() {
            // 仅在满足条件时初始化
            let trajectory_count = self.trajectory_storage.count_relevant().await;
            if trajectory_count < 10 {
                return false; // 轨迹不足，拒绝初始化
            }
            let mut engine = SkillEvolutionEngine::new();
            if let Some(p) = llm_provider { engine.set_llm_provider(p); }
            if let Some(s) = sandbox { engine.set_sandbox(s); }
            *engine_guard = Some(engine);
        }
        true
    }

    /// 仅在引擎已初始化且轨迹充足时执行进化
    pub async fn try_evolve(&self, skill: &Skill) -> Option<SkillModification> {
        let guard = self.evolution_engine.read().await;
        guard.as_ref()?.run(skill, &self.get_trajectories()).await
    }
}
```

**改动要点**：

1. `SkillState` 中的进化引擎改为 `Option` 包裹，初始为 `None`
2. 添加 `ensure_evolution_engine()` 方法，接收 LLM provider 和 sandbox 作为参数（而非引擎内部持有）
3. 添加 trajectory 数量阈值（如 ≥ 10 条相关轨迹），不足时拒绝初始化
4. `try_evolve()` 为惰性调用，仅在引擎已初始化时执行
5. 移除 `SkillState` 启动时对 `SkillEvolutionEngine::new()` 的直接依赖

**风险与成本**：

- 改动成本：**中**。需要调整 `SkillState` 结构体和所有引用 `evolution_engine` 的代码路径
- 向后兼容：API 不变（`try_evolve()` 替代原有的直接调用）
- 关键风险：需要确认 `LlmEvolutionProvider` 和 `SandboxExecutor` trait object 的生命周期管理
- 测试需求：验证懒初始化路径和不满足条件时的降级行为

---

### 5.4 双重存储同步优化方案 (对应缺陷 3.1)

**问题现状**

前端存在两个完全独立的 Zustand Store，各自维护技能数据：

| Store                    | 职责                           | 数据来源                                                        |
| ------------------------ | ------------------------------ | --------------------------------------------------------------- |
| `useSkillStore`          | 技能 CRUD、市场、提案          | `invoke("list_skills")` → `loadSkills()`                        |
| `useSkillExtensionStore` | 扩展点合并（导航/页面/命令等） | `invoke("list_skills")` → `fetchSkills()` + `mergeExtensions()` |

**同步路径**（每个写操作都触发）：

```
skillStore 写操作 (toggle/install/uninstall/create/patch/edit)
  ↓
skillStore 内部 set({ skills: ... })
  ↓
syncExtensionStore()  ← 第 2 次 invoke("list_skills")
  ↓
useSkillExtensionStore.fetchSkills()  → mergeExtensions() → set(...)
```

**并发问题**：

- `loadSkills()` 和 `fetchSkills()` 可以同时运行（都调用 `invoke("list_skills")`）
- `listen("skill-state-changed")` 触发 `loadSkills()` + `invalidateLifecycleCache()`
- `listen("skill:file-changed")` 触发 `refreshSkill()` → 也调用 `invoke("list_skills")`
- 极端情况下，同一个 Tauri IPC 路径被并发调用 3 次

**影响评估**

| 维度       | 评估                                                                       |
| ---------- | -------------------------------------------------------------------------- |
| 网络开销   | 中。每次技能变更触发 2 次 `list_skills` IPC（skillStore + extensionStore） |
| 数据一致性 | 低。两个 store 的 `skills` 数组可能短暂不同步（但在下一帧会收敛）          |
| 修复紧迫性 | P3（优化，不影响功能正确性）                                               |

**推荐方案：单向数据流 + 派生 Store**

**方案 A：消除双 IPC（推荐，低成本）**

将 `syncExtensionStore()` 改为直接传递已在内存中的 skills 数据，避免重复 IPC：

```typescript
// skillExtensionStore.ts 新增方法
syncFromSkills: (skills: Skill[]) => {
  const merged = mergeExtensions(skills);
  set({ skills, ...merged, loading: false });
}, // skillStore.ts 修改 syncExtensionStore
  async function syncExtensionStore(): Promise<void> {
    const { useSkillExtensionStore } = await import("@/stores");
    // 直接传递当前内存中的 skills，无需再 invoke
    useSkillExtensionStore.getState().syncFromSkills(get().skills);
  };
```

**方案 B：Zustand subscribe（更彻底的解耦）**

```typescript
// 在 skillExtensionStore 初始化时订阅 skillStore
useSkillStore.subscribe((state, prevState) => {
  if (state.skills !== prevState.skills) {
    useSkillExtensionStore.getState().syncFromSkills(state.skills);
  }
});
```

这样 `skillStore` 的所有写操作路径都可以删除手动 `syncExtensionStore()` 调用。

**改动要点**：

1. `skillExtensionStore` 新增 `syncFromSkills(skills: Skill[])` 方法
2. `skillStore` 中所有调用 `syncExtensionStore()` 的地方改为直接传 skills
3. 可选：添加 Zustand subscribe 自动同步，删除所有手动调用
4. 保留 `fetchSkills()` 方法（独立刷新场景，如系统启动），但内部也同步更新 skillStore

**风险与成本**：

- 改动成本：**低**（方案 A 约 20 行，方案 B 约 30 行）
- 向后兼容：完全兼容，所有公开方法签名不变
- 收益：每次技能变更减少 1 次 Tauri IPC 调用
- 测试需求：验证 dual-store 同步时序（toggle → enable/disable → extension refresh）

---

## 六、本次修复记录（第二步补充）

### 已实施的极低风险修复

| 编号    | 缺陷                    | 文件                                             | 改动类型                                                                                                                                                                  |
| ------- | ----------------------- | ------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| P3-2.19 | 缓存 TTL 硬编码         | `src/lib/skillLifecycle.ts`                      | `LIFECYCLE_CACHE_TTL_MS` → 可配置变量 + `getLifecycleCacheTtl()` / `setLifecycleCacheTtl()` API。传入 0 或负数禁用缓存                                                    |
| P3-2.20 | eventBus FIFO 驱逐      | `src/lib/skillEventBus.ts`                       | FIFO → LRU：每个 key 记录 `lastAccess` 时间戳，`emit` 和 `on` 均更新访问时间。驱逐时按 `lastAccess` 升序 + `handlers.size` 升序排序，优先驱逐最久未访问且监听器最少的 key |
| P3-2.25 | notification 类型不匹配 | `src/components/skill/SkillSandboxContainer.tsx` | `notify` 函数中 error 类型使用 `notification.error({ message: title, description: message })`，对齐 antd `NotificationArgsProps`。其他类型保持不变                        |
| P3-2.24 | 锁粒度过粗              | `src-tauri/src/commands/skill_decomposition.rs`  | `tokio::sync::Mutex` → `tokio::sync::RwLock`。`cleanup_expired` 和 `set` 使用 `write()` 锁，`get` 使用 `read()` 锁。读操作不再互斥阻塞                                    |

### 架构级方案总结

| 问题              | 推荐方案                           | 改动成本 | 优先级建议 |
| ----------------- | ---------------------------------- | -------- | ---------- |
| 5.1 追踪器 LRU    | conversation 级 LRU + TTL 定时清理 | 低       | P3         |
| 5.2 热重载        | RwLock 替代 LazyLock + 重载命令    | 低       | P3         |
| 5.3 进化引擎解耦  | `Arc<RwLock<Option<T>>>` 懒初始化  | 中       | P3         |
| 5.4 双 Store 同步 | 消除双 IPC：内存传递 skills        | 低       | P3         |

所有方案均保持向后兼容，不改变现有 API 契约。
